{
  "lock": false,
  "tasks": {
    "pre-commit": "deno task fmt && deno task check && deno task test",
    "test": "deno test -A **/*_test.ts",
    "check": "deno check **/*.ts && deno fmt --check && deno lint && luacheck lua/*",
    "fmt": "deno fmt && stylua ./lua"
  }
}
